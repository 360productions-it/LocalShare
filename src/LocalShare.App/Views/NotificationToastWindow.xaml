<Window x:Class="LocalShare.App.Views.NotificationToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Received Notification"
        Height="110" Width="360"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Storyboard x:Key="SlideInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.0" To="1.0" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="40" To="0" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <Window.RenderTransform>
        <TranslateTransform/>
    </Window.RenderTransform>

    <Border Background="#1E293B" BorderBrush="#10B981" BorderThickness="1.5" CornerRadius="12" Padding="14" Cursor="Hand" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Color="#000000" Direction="270" Opacity="0.6" ShadowDepth="6"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Icon -->
            <Border Grid.Column="0" Width="44" Height="44" CornerRadius="10" Background="#0F172A" Margin="0,0,12,0" VerticalAlignment="Center">
                <TextBlock Text="📥" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,8,0">
                <TextBlock x:Name="TxtTitle" Text="File Received" Foreground="#34D399" FontSize="13" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="TxtFileName" Text="filename.ext" Foreground="White" FontSize="12" FontWeight="SemiBold" Margin="0,2,0,2" TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="Click to view file in folder" Foreground="#94A3B8" FontSize="10"/>
            </StackPanel>

            <!-- Close Button -->
            <Button Grid.Column="2" Content="❌" Background="Transparent" Foreground="#64748B" BorderThickness="0" FontSize="10" VerticalAlignment="Top" Padding="4" Click="CloseButton_Click" Cursor="Hand"/>
        </Grid>
    </Border>
</Window>
